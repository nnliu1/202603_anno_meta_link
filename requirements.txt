# Core Jupyter Environment
jupyter
notebook

# LinkML Ecosystem
linkml                # The main toolset (gen-json, gen-python, etc.)
linkml-runtime        # Needed to run the generated Python code
linkml-renderer       # For visualizing schemas as diagrams (optional but cool)

# OEP and Metadata Tools
omi                   # Open Metadata Integration library for OEP
frictionless          # OEP metadata is based on Frictionless Data packages
rdflib                # If you're doing any RDF/Semantic Web work (Tutorial Part 4)

# Utilities
pyyaml                # For handling the .yaml schema files
pydantic              # LinkML can generate Pydantic models for OEP